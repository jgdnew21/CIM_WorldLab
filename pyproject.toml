[project]
name = "cim-worldlab"
version = "0.0.1"
description = "CIM WorldLab kernel (teaching virtual world)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
